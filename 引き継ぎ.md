# 日めくりカレンダー 引き継ぎ書

最終更新: 2026-06-17

新規チャット再開時は **このファイルを `@引き継ぎ.md` で添付** するか、冒頭に「`AI-WORK/Daily-Calendar/引き継ぎ.md` を読んで再開」と書く。

---

## 正本（ここだけ触る）

```
/Users/isamuosasue/AI-WORK/Daily-Calendar/
```

| 項目 | 内容 |
|------|------|
| 本番 URL | https://day-calendar-one.vercel.app/ |
| GitHub | https://github.com/touroku1069del/Day-Calendar |
| ブランチ | `main`（push → Vercel 自動デプロイ） |
| 構成 | 単一 `index.html`（React + Tailwind CDN + Babel + localStorage） |

## エージェント分担

| 担当 | 役割 |
|------|------|
| **Cursor / Antigravity** | 機能開発・本番修復・git push |
| **Claude Code** | launchd・iCloud 同期など macOS 自動化のみ |
| **Gemini Canvas** | 原型のみ。本番の直接編集はしない |

**旧フォルダは触らない:** `Claude Code/日めくりカレンダー`, `Cursor/my-project/Daily Calendar`, `Antigravity/日めくりカレンダー`

---

## いまの状態（MVP として動作中）

- iPhone でホーム画面登録して日常利用中
- 今日の日付・言葉・目標（最大4件）・COMING UP が動く
- データはブラウザ `localStorage`（端末内のみ、同期なし）
- 写真スペースは **プレースホルダーのみ未実装**

### 確定済み UX 仕様

- 目標: **デフォルト4件プリセット**（深く呼吸する、紙に書く／未来の自分との約束を守る／だから優先順位を守る／全てに感謝します）を**タップして直接書き換え**。**＋ボタン・鉛筆は廃止**（書き換えるだけ）。丸チェックで完了。**3件表示・4件目スクロール**。空にすると空欄（プレースホルダー「目標 N」）。毎日デフォルトへリセット（2026-06-18 改善。＋方式は同日廃止）
- ヘッダー色: **濃いロイヤルブルー `#2d4ed6`**（旧 `#3b66de` から変更。2026-06-18）
- COMING UP: 初期は空白（「予定なし」は使わない）
- 長押し削除は **廃止**（スクロール誤削除のため）

---

## 直近で起きた障害と修正（2026-06-17）

### 症状
Vercel 本番が真っ白。Gemini から開けなくなった。

### 原因
```text
Failed to resolve module specifier "react/jsx-runtime"
```
Babel の自動 JSX 変換が `react/jsx-runtime` を要求するが、import map に無かった。CDN 側の挙動変化で突然発生（コード未変更でも起きうる）。

### 修正内容（本番反映済み）
- import map に `react/jsx-runtime` / `react/jsx-dev-runtime` 追加
- `export default function App()` → `function App()` に変更
- 認証待ちで `setLoading(false)` を呼ぶ（青画面固まり防止）
- 目標の state 更新を `setTodayData(prev => ...)` に統一

コミット: `b45f3d3` on `main`

---

## 写真の最新版を再現（2026-06-18）

### 経緯
iPhone表示の「最新版」スクショだけが残り、実ファイルは消失。調査した全場所（ローカル各フォルダ／GeminiバックアップのDOM記録／Firebase Hosting `.web.app`・`.firebaseapp.com`＝両方404＝未デプロイ／Vercel）に **MVP版** と **Premium版（`Desktop/(オリ) Macスクショ/Gemini.html`：英語ラベル・本物Firestore・愛犬写真・3件固定）** しか無く、写真の版は復元不能。→ 現行MVPを土台に写真から再現した。

### 実装（現行 `index.html`）
- ヘッダー他の青を `#3b66de` → `#2d4ed6` に統一
- 目標: 最大4件入力／3件表示（`max-h-[176px]`・4件目スクロール）
- 追加方式を「常時入力行＋Enter」→「鉛筆で編集→＋ボタンで行追加→鉛筆(保存)で確定」に変更（Enter追加も併用）
- **その後同日中に再改善**：＋ボタンと鉛筆を廃止。デフォルト4件プリセットをタップして直接書き換える方式に変更（設計書＝`docs/superpowers/specs/2026-06-18-goals-preset-inline-edit-design.md`）

### 検証
ヘッドレスChromeで通常表示・編集モードを撮影し写真一致を確認。設計書＝`docs/superpowers/specs/2026-06-18-daily-calendar-reproduce-design.md`。

---

## 次にやること（優先順）

### 1. 写真スペース（ユーザー希望・未着手）
- iPhone の写真ライブラリから選択
- **日付ごとに1枚**（`dateKey` 単位。パターン A）
- 保存は **IndexedDB**（localStorage は容量不足のため不可）
- タップで選択 → ヘッダー右半分に表示

### 2. PWA 強化（任意）
- `manifest.json`、アイコン、ホーム画面をよりアプリらしく

### 3. Firebase 本番連携（任意）
- 複数端末でデータ同期が必要になったら
- ローカルは現在 Firebase モック（`data:` URL）

---

## ローカル開発

```bash
cd "/Users/isamuosasue/AI-WORK/Daily-Calendar"
python3 -m http.server 8765
# http://localhost:8765/index.html
```

デプロイ:
```bash
git add index.html
git commit -m "説明"
git push origin main
```

---

## 新規チャット用プロンプト例

```
/Users/isamuosasue/AI-WORK/Daily-Calendar/引き継ぎ.md を読んで再開。
写真スペース（日付ごと1枚・IndexedDB）の実装から進めて。
正本フォルダのみ編集し、完了後 GitHub に push して Vercel 反映まで。
```

---

## 補足

- ユーザーは `AI-WORK/` を全エージェント共通のプロジェクト置き場に統一中
- Claude Code のスキルブラックボックス化、過剰なクレジット消費破産を避け、クレジット無料のCursor/Antigravity を開発主体にする方針、高度で複雑なコードはClaude Codeへ支援を依頼する
- 将来的にはHermes Agentから各プロジェクトに最適なAIをAPIで使用することでAPIクレジット最小化を図る
